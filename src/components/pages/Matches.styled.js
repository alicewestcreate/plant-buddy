import { styled } from "@mui/system";

export const MainHome = styled("h1")({
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
    padding: "1rem"
})