import { styled } from '@mui/material/styles';


export const Box = styled("div")({
    flexGrow: 1,
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
})